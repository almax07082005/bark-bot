name: 'bark-bot-app'

services:
  db:
    container_name: 'bark-bot-db'
    tty: true
    image: 'postgres:18.2'
    environment:
      - 'POSTGRES_DB=bark-bot-db'
      - 'POSTGRES_USER=${DATASOURCE_USERNAME}'
      - 'POSTGRES_PASSWORD=${DATASOURCE_PASSWORD}'
    ports:
      - '${DATASOURCE_HOST_PORT}:${DATASOURCE_CONTAINER_PORT}'
    volumes:
      - 'bark-bot-db-volume:/var/lib/postgresql'
    restart: always

  app:
    container_name: 'bark-bot-app'
    tty: true
    image: 'almaxgood/bark-bot:latest'
    env_file:
      - .env
    depends_on:
      - db
    restart: always

  loki:
    container_name: 'bark-bot-loki'
    image: 'grafana/loki:3.6.0'
    user: '0'
    command: -config.file=/etc/loki/config.yaml
    volumes:
      - './loki/loki-config.yaml:/etc/loki/config.yaml:ro'
      - 'loki-data:/loki'
    restart: always

  alloy:
    container_name: 'bark-bot-alloy'
    image: 'grafana/alloy:v1.14.0'
    command: run --server.http.listen-addr=0.0.0.0:12345 --storage.path=/var/lib/alloy/data /etc/alloy/config.alloy
    volumes:
      - './loki/alloy-config.alloy:/etc/alloy/config.alloy:ro'
      - '/var/run/docker.sock:/var/run/docker.sock:ro'
      - 'alloy-data:/var/lib/alloy/data'
    depends_on:
      - loki
    restart: always

  grafana:
    container_name: 'bark-bot-grafana'
    image: 'grafana/grafana:11.4.0'
    environment:
      - 'GF_SECURITY_ADMIN_USER=admin'
      - 'GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD}'
      - 'GF_AUTH_ANONYMOUS_ENABLED=false'
      - 'GF_PATHS_PROVISIONING=/etc/grafana/provisioning'
    ports:
      - '3000:3000'
    volumes:
      - './grafana/provisioning:/etc/grafana/provisioning:ro'
      - 'grafana-data:/var/lib/grafana'
    depends_on:
      - loki
    restart: always

volumes:
  bark-bot-db-volume:
  loki-data:
  alloy-data:
  grafana-data: