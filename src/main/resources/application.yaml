spring:
  application:
    name: bark-bot
  jpa:
    hibernate:
      ddl-auto: validate
  datasource:
    driver-class-name: org.postgresql.Driver
    url: 'jdbc:postgresql://bark-bot-db:${DATASOURCE_CONTAINER_PORT}/bark-bot-db'
    username: ${DATASOURCE_USERNAME}
    password: ${DATASOURCE_PASSWORD}
  flyway:
    url: 'jdbc:postgresql://bark-bot-db:${DATASOURCE_CONTAINER_PORT}/bark-bot-db'
    user: ${DATASOURCE_USERNAME}
    password: ${DATASOURCE_PASSWORD}
    baseline-on-migrate: true
    validate-on-migrate: true
app:
  bot-token: ${PROD_BOT_TOKEN}
  thread-pool-size: ${THREAD_POOL_SIZE}
  thread-timeout-ms: ${THREAD_TIMEOUT_MS}
  support-username: ${SUPPORT_USERNAME}
  support-message-template: ${SUPPORT_MESSAGE_TEMPLATE}